# Manifest Reader Frontend

This folder keeps frontend assets separated from the Spring Cloud backend.

## Structure

- `admin/`: admin console pages and assets.
- `client/`: client-facing pages and assets.

The current stage uses static HTML/CSS/JavaScript so the admin UI can be opened directly without installing frontend dependencies.

