spring:
  application:
    name: manifest-reader-llm-task
  config:
    import: optional:classpath:dev.yml

server:
  port: 8084

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui.html
